package Herds;

// Movable interface representing objects that can move
public interface Movable {

    // Method signature for moving an object by the specified amount in the x and y directions
    void move(int dx, int dy);
}
